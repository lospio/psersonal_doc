1. 为什么要把函数模板放在头文件？
	在编译阶段，对于函数模板必须要知道完整的定义才可以实例化（生成相应的代码），相比之下非模板函数只需要函数声明即可，在链接阶段再去寻找对应的函数，找不到就报错，但是这种做法不适用于模板函数。模板函数只有在使用的时候才会实例化，生成相应的代码，多个不同的类型使用会生成不同的代码，所以必须在预处理阶段就处理，无法像普通函数一样在链接阶段处理（因为其他地方的实现不一定是相同类型的，比如需要一个f<int>()）。
	将函数模板放在头文件当中，头文件会在预处理阶段将文件内容复制到cpp文件中。如果多个cpp文件存在多个相同声明的函数模板，编译器分别编译多个翻译单元（cpp文件），将使用到的模板实现记录下来，再在链接阶段对比多个记录，是否使用了相同的函数模板实现，将多个使用的函数模板实例合并起来，并且随机使用其中一个实现，丢弃其他的实例，如果实现不同，将导致严重的后果。