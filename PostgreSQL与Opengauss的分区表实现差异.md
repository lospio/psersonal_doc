## 元数据模型：分散继承 vs 集中专用

### PostgreSQL：基于继承模型的分散元数据

PostgreSQL **没有专用分区元数据表**，而是复用现有系统表组合表达分区逻辑：

| 系统表                    | 作用         | 分区相关字段                                   |
| ---------------------- | ---------- | ---------------------------------------- |
| `pg_class`             | 存储所有关系对象   | 父表与每个分区均为独立 `relkind = 'r'` 条目           |
| `pg_inherits`          | 表示继承关系     | `inhparent`（父表 OID），`inhrelid`（子分区 OID）  |
| `pg_partitioned_table` | **仅父表有记录** | `partstrat`（策略：r/l/h），`partattrs`（分区键列号） |
| `pg_constraint`        | 存储约束       | 每个分区有一条 `CHECK` 约束，隐式定义边界                |
### openGauss：专用集中式元数据

openGauss 引入 **`pg_partition`** 作为**唯一权威分区元数据源**：

|字段|类型|说明|
|---|---|---|
|`parttype`|`"char"`|`'p'`=父表,`'r'`=范围,`'l'`=列表,`'h'`=哈希|
|`parentid`|`oid`|父对象 OID（一级分区指向父表，二级分区指向一级分区）|
|`partkey`|`int2vector`|分区键列在父表中的属性编号（如`{1,2}`）|
|`partboundary`|`text`|**显式结构化边界**（如`"2024-01-01,2025-01-01"`）|

 - 优点：信息集中，支持 O(1) 分区查找；天然适配自动扩分区、SPLIT 等操作。 
 - 缺点：与 PostgreSQL 继承模型不兼容。

---

## 存储结构与物理布局

| 维度         | PostgreSQL                                        | OpenGauss                                      |
| ---------- | ------------------------------------------------- | ---------------------------------------------- |
| **物理表组织**  | 每个分区是独立堆表，拥有自己的`pg_class`条目和数据文件                  | 同左，但支持**分区级表空间**（`pg_partition.reltablespace`） |
| **索引支持**   | 仅**本地索引**（每个分区独立索引）  <br>父表索引为虚拟索引（`relkind='I'`） | 支持**本地索引 + 全局索引**  <br>全局索引跨所有分区，保证唯一性         |
| **列存支持**   |  原生不支持                                            | 支持**列存分区表**（`orientation = column`）            |
| **数据文件路径** | `base/<dboid>/<relfilenode>`（每个分区独立）              | 同左，但可指定不同表空间                                   |
#todo 
 **全局索引实现关键**：   openGauss 扩展了 `ItemPointer` 结构，编码 `(partition_id, block_id, offset)` 以定位跨分区物理行。

---

## 分区键定义与语义

### PostgreSQL 分区键

- 通过 `pg_partitioned_table.partattrs` 存储**列号数组**（如 `{1,3}` 表示第1、3列）。
- **限制**：
    - 不支持表达式分区键（如 `(date_part('year', ts))` 需用生成列）；
    - 分区键必须包含在主键/唯一约束中，否则无法创建唯一索引。
### openGauss 分区键

- 同样通过 `pg_partition.partkey` 存储列号。
- **增强**：
    - 支持**间隔分区（INTERVAL）**，自动按时间间隔创建新分区；
    - 允许**二级分区**（如 RANGE-LIST），元数据扁平化管理；
    - 分区键更新时，若启用 `ROW MOVEMENT`，自动迁移行到新分区。

---

## 功能支持对比

| 功能           | PostgreSQL        | OpenGauss                      |
| ------------ | ----------------- | ------------------------------ |
| **分区类型**     | RANGE, LIST, HASH | RANGE, LIST, HASH,**INTERVAL** |
| **多级分区**     | 支持（递归继承）          | 支持（扁平链式，`pg_partition`统一管理）    |
| **自动扩分区**    | 不支持               | 插入越界数据自动创建新分区                  |
| **分区维护 DDL** | `ATTACH/DETACH`   | `SPLIT`,`MERGE`,`DROP`,`ADD`   |
| **全局索引**     | 仅本地索引             | 原生支持                           |
| **分区键更新**    | 报错                | 支持行迁移（需`ENABLE ROW MOVEMENT`）  |
| **默认分区**     | `DEFAULT`分区       | 支持，且可 SPLIT                    |
| **动态剪枝**     | 仅静态（规划期）          | 支持执行期动态剪枝                      |
| **列存分区**     | 不支持               | 支持                             |

---

