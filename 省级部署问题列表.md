---
ctime: 2023-02-22 18:43
tags: log
author: dengjie
alias: 
---
# 宁夏统信环境部署问题

#### pre_install
-   预安装时pip安装失败
    -   具体现象：安装python3-pip时提示如下错误，即安装时link出错，/usr/bin/pip3这个文件已存在
		```Bash
failed to link /usr/bin/pip3 -> /etc/alternatives/pip3: /usr/bin/pip3 exists and it is either not a symlink or --keep-foreign was set and link points outside /etc/alternatives
	```
- 现象分析：已有pip3，且是真实文件不是软连接
- 解决办法：mv命令给/usr/bin/pip3和/etc/alternatives/pip3这两个文件改名，再重新安装

-   预安装时ansible安装失败(pip3 安装)
    -   具体现象：使用pip3安装的时候出现如下错误![[Pasted image 20230322111451.png]]
    -   现象分析：验证环境的pip(21.3)和包内提供的pip(9.0.3)版本不同，使用验证环境的pip(21.3)下载的ansible包不能被现场安装的pip(9.0.3)安装。验证环境系统yum源中的pip版本是9.0.3，应该是安装后，后续手动升级了
    -   解决办法，使用rpm包安装ansible，验证环境使用repotrack命令下载ansible的全量rpm包复制到现场环境，再使用rpm命令强制安装所有全量包
        

#### system_check
-   防火墙端口打开失败
    -   具体现象：打开防火墙端口流程中，装有ansible的机器防火墙端口打开失败![](https://secure2.wostatic.cn/static/2oxrjNtTGVSUcxiJuH9Ga8/%E9%98%B2%E7%81%AB%E5%A2%99%E5%BC%80%E5%90%AF%E5%A4%B1%E8%B4%A5.jpg?auth_key=1679454520-mWvjzj46RF8AwA4NpasjUE-0-a5bfc3c7ae60385face6dbaf636d9e43) 
    -   现象分析：应该是由于强制安装python3造成的
    -   解决办法：现场解决办法是直接关闭了防火墙，并跳过此步骤
        
-   yum命令失效、ntp服务无法开启 
    -   具体现象：无法使用yum命令，提示缺少python-dnf模块，chrony无法开启ntp同步时间![[Pasted image 20230322111540.png]]![[Pasted image 20230322111555.png]]
    -   现象分析：yum和ntp失效与pre_install里强制安装有关
    -   解决办法：yum：使用rpm包安装python-dnf全量包(与ansible类似)，之后使用pip将原有的dnf模块卸载。ntp：安装了ntp，然后发现它启动失败，于是stop & disable，然后手动卸载了chrony，重新跑了一遍就过了

#### install
-   使用pip安装patroni出错
    -   具体现象：安装patroni时需要安装依赖psutil，psutil在提供的包中是源码编译安装，编译时出错。(这些包是在验证环境上通过pip download自动下载的)
        ![[Pasted image 20230322111612.png]]
        
    -   解决方法
        网上找了一个对应版本的.whl的包进行替换，通过pip 安装whl包的方式安装
-   install_depends
    -   具体现象
        使用yum安装依赖软件时有如下报错，提示没有python-dnf模块
		```Bash
Could not import the dnf python module using /usr/bin/python3.7 (3.7.9 (default, Feb 11 2022, 15:14:57) [GCC 7.3.0]). Please install `python3-dnf` package or ensure you have specified the correct ansible_python_interpreter.
		```
- 现象分析：
    yum命令使用了系统原有的python3.7，导致没有python-dnf模块
- 解决方法：
    在ansible的task中指定使用的python为我们提供的python3

#### start_verify
-   pg启动失败，缺少.so文件
    -   具体现象
	```Bash
libicui18n.so.60 => not found
libicuuc.so.60 => not found
	```
- 现象分析：
    可能是现场系统已装有libicu且版本与所依赖版本不同，在安装依赖软件时跳过了libicu的安装，导致缺少一些文件
- 解决方法：
     从验证环境中复制对应的文件到现场

-   236节点上pg启动失败
    -   具体现象：
        备库数据存放路径缺失，且创建好路径，数据库启动以后重跑流程时还会出现此现象，实际上该路径也确实被删除了
    -   解决办法
        创建相关路径，重跑流程

# 天津部署
### 系统版本
```bash
4.19.90-24.4.v2101.ky10.x86 64 (KYLINSOFTlocalhost.localdomain) (gcc version 7.3.9 (GCC)) #1 SMP Mon Mav 24 12:14:55 CST 2021
```
### 问题：
#### 1.生成配置文件的脚本名字错误
**问题** 安装包内提供的名字为GenerateInventroy.py，应该是GenerateInventory.py

#### 2.生成配置文件的脚本内使用了非ascii码字符
**问题** 
![[Pasted image 20230314145250.png]]
**原因及解决办法** 应该是脚本内的中文注释，删除注释或者文件开头添加注释让python解释器使用utf-8编码

#### 3.安装ansible后影响了防火墙等其他软件的使用
**问题** 使用pre_install安装了ansible以后启动部署脚本，发现防火墙不能正常运行
![[Pasted image 20230314145959.png]]
**原因及解决办法** 从图上可知防火墙缺少libip4tc.so.2文件，该文件是iptables-libs软件提供的，这里是缺少是因为使用pre_install安装ansible的时候强制安装了另一个版本的iptables-libs，该版本没有提供这个文件。解决办法，使用现场的yum源reinstall了iptables-libs就可以了

#### 4. 开启ntp端口失败
**问题** 
![[Pasted image 20230314153142.png]]
**原因及解决办法** ntp端口已经打开了，且已经做过了时间同步了，这一步以及后面的时间同步配置跳过

#### 5. 使用pip安装whl包失败
**问题** 在使用pip安装whl包的步骤中出现如下问题
![[Pasted image 20230314155249.png]]
**原因及解决办法** 因为pre_install时有部分软件被强制安装了其他版本，导致pip不能正常运行，重新安装gcc后可运行

#### 6.使用yum安装依赖软件时失败
**问题** 在使用yum安装时所有节点都出现了问题
```bash
fatal: [10.226.90.232]: FAILED! => {"changed": false, "failures": [], "msg": "Depsolve Error occured: \n 问题 1: cannot install both mpfr-3.1.6-3.ky10.x86_64 and mpfr-4.1.0-1.ky10.x86_64\n  - package mpfr-devel-3.1.6-3.ky10.x86_64 requires mpfr = 3.1.6-3.ky10, but none of the providers can be installed\n  - package gcc-c++-7.3.0-20190804.35.p02.ky10.x86_64 requires libmpfr.so.6()(64bit), but none of the providers can be installed\n  - conflicting requests\n  - problem with installed package gcc-c++-7.3.0-20190804.35.p02.ky10.x86_64\n 问题 2: cannot install both json-c-0.13.1-6.ky10.x86_64 and json-c-0.15-1.ky10.x86_64\n  - package json-c-devel-0.13.1-6.ky10.x86_64 requires json-c(x86-64) = 0.13.1-6.ky10, but none of the providers can be installed\n  - package libreport-2.10.1-9.p02.ky10.x86_64 requires libjson-c.so.5()(64bit), but none of the providers can be installed\n  - package libreport-2.10.1-9.p02.ky10.x86_64 requires libjson-c.so.5(JSONC_0.14)(64bit), but none of the providers can be installed\n  - conflicting requests\n  - problem with installed package libreport-2.10.1-9.p02.ky10.x86_64\n 问题 3: cannot install both openldap-2.4.50-3.ky10.x86_64 and openldap-2.4.50-2.ky10.x86_64\n  - package openldap-devel-2.4.50-3.ky10.x86_64 requires openldap = 2.4.50-3.ky10, but none of the providers can be installed\n  - package openldap-clients-2.4.50-2.ky10.x86_64 requires openldap = 2.4.50-2.ky10, but none of the providers can be installed\n  - conflicting requests\n  - problem with installed package openldap-clients-2.4.50-2.ky10.x86_64\n 问题 4: package krb5-devel-1.17-9.ky10.x86_64 requires krb5-libs(x86-64) = 1.17-9.ky10, but none of the providers can be installed\n  - cannot install both krb5-libs-1.17-9.ky10.x86_64 and krb5-libs-1.18.2-1.ky10.x86_64\n  - package openssl-devel-1:1.1.1f-4.p04.ky10.x86_64 requires krb5-devel, but none of the providers can be installed\n  - package krb5-1.18.2-1.ky10.x86_64 requires libkdb5.so.10()(64bit), but none of the providers can be installed\n  - package krb5-1.18.2-1.ky10.x86_6
```
**原因及解决办法** 安装包内提供的版本与现场操作系统的不符合。解决办法是少数几个现场yum源中没有的包使用安装包内的rpm文件安装其他的都用yum源中的包安装
重新安装的包有
```
python3-cryptography
python3-devel
python3-libs
python3-rpm-generators
python3-rpm-macros
python3-setuptools
```