# 产品名称

## 概要设计说明书（Table Identity 属性）

产品版本：<5.0.0>

### 修订记录

| 版本号 | 发布日期 | 描述 | 作者 | 批准 |
| :--- | :--- | :--- | :--- | :--- |
| <1.0> | 2026-02-05 | 初稿撰写 | 技术文档组 | 架构委员会 |
| | | | | |
| | | | | |

### 变更记录

| 变更编号 | 变更日期 | 变更项 | 描述 | 基线版本 | 变更请求编号 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 2026-02-05 | Identity属性 | 新增MSSQL兼容模式下Table Identity属性支持 | V4.2 | REQ-2026-001 |

### 目录

1. [功能规格](#1-功能规格)
    1. [功能简述](#11-功能简述)
    2. [参数与类型](#12-参数与类型) 
    3. [支持与限制](#13-支持与限制)
2. [语法语义与Hook设计](#2-语法语义与Hook设计)
    1. [语法解析改造](#21-语法解析改造)
    2. [Hook设计方案](#22-Hook设计方案)
    3. [元数据存储](#23-元数据存储)
3. [执行流程](#3-执行流程)
    1. [创建流程](#31-创建流程)
    2. [插入流程](#32-插入流程)
4. [其他说明](#4-其他说明)
    1. [接口说明](#41-接口说明)
    2. [内存管理](#42-内存管理)
    3. [安全](#43-安全)
    4. [性能](#44-性能)
    5. [升级管理](#45-升级管理)

---

## 概要设计说明书

Table Identity属性（MSSQL兼容性）

## 1 功能规格

### 功能简述

Identity属性是数据库兼容性中的功能，允许在创建表时将某一列定义为标识列。该列的值由数据库内部序列根据指定的种子（Seed）和增量（Increment）自动生成。

本功能旨在实现与SQL Server `CREATE TABLE ... IDENTITY` 语法的高度兼容，包括对 `IDENTITY_INSERT` 开关的支持以及相关系统函数的适配。

### 参数与类型

#### 参数定义
* **Seed (种子)**：标识列的起始值。若未指定，默认为 1。
* **Increment (增量)**：每次生成的步长。若未指定，默认为 1。

#### 支持的数据类型
Identity属性仅支持精确数值类型。若列的数据类型不在下表范围内，建表时应报错。

| 数据类型 | 字节数 | 说明 | 范围 (Approx.) |
| :--- | :--- | :--- | :--- |
| **tinyint** | 1 | 微整型 | 0 ~ 255 |
| **smallint** | 2 | 小整型 | -32,768 ~ 32,767 |
| **int** | 4 | 整型 | -2,147,483,648 ~ 2,147,483,647 |
| **bigint** | 8 | 长整型 | -9.22E18 ~ 9.22E18 |
| **decimal(p,0)** | Varies | 定点数 | 仅支持标度(Scale)为0的定义 |
| **numeric(p,0)** | Varies | 定点数 | 仅支持标度(Scale)为0的定义 |

### 支持与限制

#### 支持特性
1.  **自动生成**：插入数据时忽略该列，系统自动填充序列值。
2.  **显式插入**：支持 `SET IDENTITY_INSERT [TableName] ON`，允许用户手动指定标识列的值。
3.  **自适应重置**：显式插入的值若大于当前序列最大值，系统应自动调整序列高水位，避免后续冲突。
4.  **系统函数**：支持 `IDENT_SEED`, `IDENT_INCR`, `IDENT_CURRENT`, `SCOPE_IDENTITY` 等函数查询元数据。

#### 限制条件
1.  **唯一性**：每个表至多只能有一个 Identity 列。
2.  **非空约束**：Identity 列隐式包含 `NOT NULL` 属性。
3.  **默认值互斥**：Identity 列不能同时定义 `DEFAULT` 约束。
4.  **不可更新**：Identity 列的值一旦生成，通常不允许通过 `UPDATE` 语句修改。
5.  **类型限制**：不支持 `FLOAT`, `REAL` 或字符类型。

## 2 语法语义与Hook设计

### 语法解析改造

需修改 SQL Parser (`gram.y`) 以识别 MSSQL 风格的列定义语法。

**核心修改点：**
* 扩展 `ColConstraintElem` 节点，增加 `IDENTITY` 关键字识别。
* 支持 `IDENTITY` 无参形式（默认1,1）及带参形式 `IDENTITY ( seed , increment )`。
* **注意**：在 Parser 阶段暂不校验类型合法性，留待语义分析阶段处理。

### Hook设计方案

为了在 PostgreSQL 内核架构上实现该功能，采用 **ParseTree Transformation Hook** 与 **Executor Hook** 相结合的方案。

#### 1. 语义转换 Hook (Transform Hook)
在 `CREATE TABLE` 的语义分析阶段（`transformCreateStmt`），拦截带有 Identity 属性的列定义：
* **动作**：将 Identity 属性转换为内核原生的 Sequence（序列）关联操作。
* **逻辑**：
    1.  提取 seed 和 increment。
    2.  构造一个隐式 Sequence（命名规则：`table_col_seq`）。
    3.  将 Sequence 的 `OWNED BY` 绑定到该列。
    4.  将该列的 `DEFAULT` 表达式设置为 `nextval('seq_name')`。
    5.  **关键差异**：在系统表中标记该列为 `attidentity = 'd'` (By Default) 或自定义的兼容性标记，以便后续 Hook 区分普通 Default 和 Identity。

#### 2. 执行器检查 Hook (ExecutorStart Hook)
用于实现 strict 的插入行为控制（即 `IDENTITY_INSERT` 逻辑）。

* **拦截点**：`ExecutorStart` 中的 `INSERT` 操作。
* **检查逻辑**：
    1.  扫描目标表是否存在 Identity 列。
    2.  **场景 A**：INSERT 语句包含了 Identity 列。
        * 检查会话级变量 `g_identity_insert_table_oid`。
        * 若变量未指向当前表，抛出错误：“Cannot insert explicit value for identity column in table '...' when IDENTITY_INSERT is set to OFF.”
    3.  **场景 B**：INSERT 语句未包含 Identity 列。
        * 检查会话级变量 `g_identity_insert_table_oid`。
        * 若变量指向当前表（即用户开启了显式插入但没给值），根据 MSSQL 行为，通常允许（使用自动生成），但在某些严苛模式下可能需警告。此处建议遵循 PG `GENERATED BY DEFAULT` 行为，允许自动生成。

### 元数据存储

* **pg_attribute**：复用或扩展 `attidentity` 字段。
    * `' '` (空): 普通列。
    * `'m'`: MSSQL 兼容模式 Identity 列（新增枚举值，区别于 PG 原生的 'a'/'d'）。
* **pg_depend**：记录 Sequence 与 Table Column 的依赖关系，确保 `DROP TABLE` 时级联删除 Sequence。
* **pg_class** (可选): 在 `reloptions` 中存储上次重置的 Seed 值（用于 `DBCC CHECKIDENT`）。

## 3 执行流程

### 创建流程

1.  **输入**：用户执行 `CREATE TABLE t1 (id INT IDENTITY(100, 5), name TEXT);`
2.  **Parser**：生成 ParseTree，标记 `id` 列带有 Identity 节点。
3.  **Transform**：
    * 创建序列 `seq_t1_id`，设置 start=100, increment=5。
    * 修改 `id` 列定义，绑定默认值为 `nextval('seq_t1_id')`。
    * 设置列的 `Not Null` 约束。
4.  **Execute**：
    * 执行建表。
    * 更新系统表 `pg_attribute`，设置 `id` 列的 `attidentity = 'm'`。

### 插入流程

1.  **输入**：用户执行 `INSERT INTO t1 (id, name) VALUES (200, 'A');`
2.  **Check**：
    * 检测到目标列 `id` 是 Identity 列。
    * 读取 Session 变量 `IDENTITY_INSERT`。
    * 若为 OFF -> **报错**。
    * 若为 ON -> **放行**。
3.  **Post-Insert Hook** (关键)：
    * 若发生了显式插入（Value = 200），且 200 > Sequence 当前值。
    * 隐式执行 `setval('seq_t1_id', 200)`，确保下一次自动生成的值为 205，防止主键冲突。

## 4 其他说明

### 接口说明

* **SET IDENTITY_INSERT**：语法级支持，修改 Session 变量。
* **DBCC CHECKIDENT**：通过存储过程实现，底层调用 `setval` 调整序列。
* **IDENT_CURRENT(table)**：查询关联序列的 `last_value`。

### 内存管理

Identity 处理主要依赖系统目录缓存（SysCache）和序列缓存（Sequence Cache）。
* **开销**：极低。每个 Identity 列仅增加一个 Sequence 对象。
* **清理**：随表 Drop 自动回收。

### 安全

* **权限控制**：只有表的所有者（Owner）或超级用户才有权执行 `SET IDENTITY_INSERT ON` 和 `DBCC CHECKIDENT`。
* **审计**：建议将显式修改 Identity 值的操作纳入审计日志。

### 性能

* **常规插入**：性能等同于原生 `SERIAL` / Sequence，属于轻量级操作。
* **显式插入**：开启 `IDENTITY_INSERT` 后的插入操作，因涉及“Post-Insert Hook”中的序列水位线检查（Reseed check），性能会略低于常规插入（约 5%-10% 损耗，视具体实现而定），但在数据迁移场景下可接受。

### 升级管理

**涉及升级管理**。
由于引入了新的系统表枚举值 (`attidentity = 'm'`) 和隐式序列逻辑：
1.  **pg_upgrade**：需确保旧版本升级上来时，原有的 `SERIAL` 列保持不变，仅新创建的 Identity 列使用新逻辑。
2.  **pg_dump/restore**：必须改造导出逻辑。对于标记为 Identity 的列，Dump 出来的 SQL 必须还原为 `IDENTITY(seed, incr)` 语法，而不是 `DEFAULT nextval(...)`，以保证跨库迁移的语义一致性。