#### log
- `log_line_prefix = '%m [%p] [%a] [%u] [%d] [%r]'` 时间 pid 应用名称 用户名 数据库 host和port
```bash
log_line_prefix = '%m [%p] '           # special values:
                                        #   %a = application name
                                        #   %u = user name
                                        #   %d = database name
                                        #   %r = remote host and port
                                        #   %h = remote host
                                        #   %p = process ID
                                        #   %t = timestamp without milliseconds
                                        #   %m = timestamp with milliseconds
                                        #   %n = timestamp with milliseconds (as a Unix epoch)
                                        #   %i = command tag
                                        #   %e = SQL state
                                        #   %c = session ID
                                        #   %l = session line number
                                        #   %s = session start timestamp
                                        #   %v = virtual transaction ID
                                        #   %x = transaction ID (0 if none)
                                        #   %q = stop here in non-session
                                        #        processes
                                        #   %% = '%'
                                        # e.g. '<%u%%%d> '
```
- `log_timezone = 'CST'`设置日志的时区
#### WAL
- `wal_buffer`
- `checkpoint_completion_target`
- `checkpoint_timeout`
- `wal_max_size`
- `max_wal_senders`
- 备机查询功能时需要修改参数：
	-  `max_standby_archive_delay`
	-  `max_standby_streaming_delay`
	- `hot_standby_feedback`可以避免删除最近失效的行。但是可能导致延迟清理，table bloat
	- `vacuum_defer_cleanup_age`不会立即清理失效的行 ，在主机设置
#### pigsty olap
- `max_connections` 100
- `shard_buffers` min(memory/400 * 100 ,64000)MB 先除400向上取整，在乘100
- `maintenance_mem` shard_buffers / 20 * 10 先除20向上取整，再乘10
- `effective_cache_size`  memory - shared_buffers
- `workmem` min( max(effective_cache_size / 3 / max_connections, 16), 1024) 
- ``