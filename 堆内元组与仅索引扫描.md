# 回顾
1. 堆表文件的内部布局
![[Pasted image 20220922222812.png]]

2. 更新数据
![[Pasted image 20220922215034.png]]
# Heap Only Tuple（HOT）
>The Heap Only Tuple (HOT) feature eliminates redundant index entries and allows the re-use of space taken by DELETED or obsoleted UPDATED tuples without performing a table-wide vacuum. It does this by allowing single-page vacuuming, also called "defragmentation".


- 消除冗余的index entries
- 重用索引元组
- 碎片整理

## 场景
![[Pasted image 20220922222748.png]]
#### 存在问题
1. 需要插入一条tuple元组和一条索引元祖。**索引元组的插入消耗了索引页的空间，而且索引元组的插入和清理都是开销很大的操作**
2. 存在同样key值的index entity。
3. 由于查找和删除链接到要回收的元组的索引条目的成本，一次一页的清理通常是不切实际的。

## HOT 工作流程
1. 通过index entity找到对应的line pointer。
2. 通过line pointer找到对应的tuple。
3. 发现HEAP_HOT_UPDATE,通过t_ctid找到tuple2。
![[Pasted image 20220922224158.png]]
![[Pasted image 20220922224222.png]]
## HOT不适用的场景
#### new tuple 和 old tuple不在相同的page
>this would destroy the desired property of being able to reclaim space with just page-local manipulations.(这将破坏仅通过页面本地操作即可回收空间的所需属性)

读多个page，降低了效率

#### 更改了indexed column
# 仅索引扫描（Index-Only Scan）
>当SELECT语句的所有的目标列都在索引键中时，为了减少I/O代价，仅索引扫描（Index-Only Scan）（又叫仅索引访问）会直接使用索引中的键值。所有商业关系型数据库中都提供这个技术，比如DB2和Oracle。PostgreSQL在9.2版本中引入这个特性。

```sql
testdb=# \d tbl 
	Table "public.tbl" 
 Column | Type | Modifiers 
--------+---------+----------- 
 id   | integer | 
 name | text    | 
 data | text    |
 
Indexes: 
	"tbl_idx" btree (id, name)
```

```sql
testdb=# SELECT id, name FROM tbl WHERE id BETWEEN 18 and 19; 
id | name 
----+-------- 
18 | Queen 
19 | Boston 


(2 rows)
```
读取目标数据表对应的可见性映射表
0：全部可见
1：非全部可见
![[Pasted image 20220922231528.png]]
#### 使用限制
1. 查询必须只引用存储在索引中的列。
2. 索引类型必须支持仅索引扫描。
	- 完全支持：B-tree
	- 部分支持： GiST and SP-GiST
	- 不支持：其他
**基本要求是索引必须物理存储，或者能够重建每个索引条目的原始数据值**


```sql
testdb=# SELECT data FROM tbl WHERE id =19; 
```
#### 使用建议
1. 对于大多数行数据不变的表。
2. 对于上述类型的查询，推荐创建index on (id , data) 。