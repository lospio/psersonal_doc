
# 01: 导出
现有数据库中的表结构和表数据分开导出，且都使用pg_dump工具进行导出
## 表结构导出
使用导出脚本进行表结构的导出
导出脚本内部具体操作：
1. 导出基本表
	- 使用该命令将表结构导出
	```
	pg_dumpall -s -h database_host -p database_port -U user > all.out
	```
		注意：此步不止导出表结构还有一些数据库其他对象，如用户，用户权限等，但是如果使用citus创建的分布式表，导出时没有shard、分布式列等分布式元信息
2. 生成citus表
	- 连接postgres数据库，查询当前pg实例中有多少个数据库(database)
		```sql
		select datname from pg_database where datistemplate = false;
		```
	- 针对每个数据库进行如下操作
		- 在当前路径下创建名为 databasename_dist.sql的文件
		- 查询该数据库中的所有复制表名称，得到复制表集合 ref_table
			```sql
			SELECT a.schema_table FROM pg_dist_partition,(select (quote_ident(pg_tables.schemaname) || '.' || quote_ident(pg_tables.tablename)) as schema_table from pg_tables) as a 
			WHERE partmethod = 'n' and pg_dist_partition.logicalrelid::regclass = a.schema_table::regclass;
			```
		- 针对每个复制表进行如下操作
			- 将创建复制表语句追加到databasename_dist.sql文件中
				```sql
				select create_reference_table('table_name');
			   ```
		- 查询该数据库中所有的分布式表的表名、分布式列、shard数，得到分布式表信息集合citus_table
			- 去除分区子表
				- 查询该数据库中所有的分区表子表的表名，得到分区表子表集合partition_subtable
				- 根据表名匹配，在citus_table集合中，去除出现在partition_subtable集合中的表及其相关信息
				- sql处理
		- 针对citus_table中的每个分布式表进行如下操作
			- 根据shard数信息将修改citus默认shard数语句追加到databasename_dist.sql文件中，即将默认shard数修改为当前表的shard数
			- 根据分布式列信息将创建分布式表语句追加到databasename_dist.sql文件中
		- 查询获取当前数据库citus默认shard数
		- 将修改citus默认shard数语句追加到databasename_dist.sql文件中，即最终将默认shard数恢复成和当前数据库一样
3. alter d_datetime	
	表结构导出脚本产物：
	all.out：包含该pg实例的所有用户、数据库、表结构等信息
	databasename_dist.sql：包含对应数据库的分布式表和复制表创建信息
		每有一个数据库就会有一个该文件，如当前pg实例中有postgres和babj_pgdb两个数据库，则导出脚本除了生成all.out外还会生成postgres_dist.sql 和 babj_pgdb_dist.sql
### 表数据导出
使用如下命令进行表数据的导出
```bash
pg_dump -a -Z 0 -d babj_pgdb --format=directory  --jobs=4 -f babj_data_dir -v
```
注意事项：
- 该命令只导出了数据，恢复数据时，需要先创建好相应的表
- 对于分区表，因为分区表的父表是一张虚表，本身不存储任何数据，所以导出的文件中没有父表相关内容，分区表的数据恢复时是每个子表单独恢复，所以恢复的目标分区表的分区范围和子表表名需要和备份的一样

# 02: 导入

## 系统参数
- max_connections 参照西安库设置
- 系统参数 打开文件数量 
- ```wal_level = minimal  # requires a restart
- maintenance_work_mem = 2GB
- max_wal_size = 20GB  # as big as you want
```
- 数据恢复过程中出错，如何继续恢复 
	- 找到完全恢复的表 
	- 找到恢复一半的表 
	- vacuum full 恢复了一半的表 
	- 将pg_restore归档目录中的已经完全恢复的表注释 
	- 校验完全恢复的表的数量和注释的表的数量是否一致
## 数据恢复

 1. 应用DDL
 ```bash
 psql -f all.out
```
 2. 校验DDL
	- 表数量
	- citus表属性
		- 分布式表和复制表数量是否正确，是否符合预期(哪些表是复制表，哪些表是分布式表)
		- 分布式表属性是否正确(分布列是否一致)
		- citus表的变更是否符合预期
### 修改表DDL
- [ ] python脚本
	- [ ] shard数 根据表大小，修改（算行数*0.25mb)
	- [ ] mettime修改

在all.out文件中有各表的DDL
对每一个存有错误数据的表进行如下操作：
- 对表中数据类型为mettime的列将列名改为temp1/temp2/temp3...
- 在表的最后新增列名为被修改列的列名且数据类型为varchar的列
```sql
alter table table_name rename column d_datetime_met to temp1;
alter table table_name rename column d_foretime_met to temp2;
alter table table_name add d_datetime_met_1 varchar;
alter table table_name add d_datetime_met_2 varchar;
```
示例如下：
修改前：
```sql
tb1(d_record_id     integer, 
				 d_datetime_met        mettime,
				 d_foretime_met        mettime);
```
修改后：
```sql
tb1(d_record_id     integer, 
				 temp1                           mettime,
				 temp2                           mettime,
				 d_datetime_met        varchar,
				 d_foretime_met        varchar);
```
注意：修改前的表中的d_datetime_met_1和修改后的表中的temp1在表中顺序一样，修改前的d_datetime_met_2和修改后的temp2也是顺序一样
### 修改分布式表元信息
原有的一些分布式表shard数设置不合理，需要进行修改，一些复制表数据量过大，需要改成分布式表

20个worker，每个内存380G
CPU逻辑核是64个
#### 修改方案：
复制表改为分布式表：
- 将数据大小为10G以上的复制表改为分布式表

分布式表shard数选择：
- 小于1G，64
- 1G-100G，128
- 100G-1000G，128 
- 1000G+，cpu核数 * worker数 / 4 (320)


- min(wk * Q,)
Q:1-cpu核数
wk * Q > 64

- shard 参考因子
	- cpu数
	- wk数
	- 表大小
	- 元组数 per shard
- shard 上限 
	- maxconcurrent queries(max_concurrent-q) * shard count(shard) < number of workers(n-wk) * max_connections per worker(max_connections-wk) shard < n-wk* max_connections-wk/max_concurrent-q 
- shard下限 
	- shard > n-wk * cpu* q(1)
	- shard > cpu 
修改后，分布式表和复制表的创建语句将写入名为citus_table.sql 的文件中
使用psql 连接到目标数据库执行该文件创建分布式表和复制表
```bash
psql -h host -p port -d babj_pgdb -f citus_table.sql
```
### 数据导入
使用pg_restore将之前pg_dump导出的数据导入到目标库中
将导出的babj_data_dir复制到新集群的cn节点上，使用如下命令进行数据导入
```bash
pg_restore -l out > db.list
nohup pg_restore -v --jobs=8 -h host -p port -d babj_rbc_pgdb babj_data_dir > restore.log 2>&1 &
```
完成后查看retore.log是否有报错信息，确认无报错信息则数据导入完成

### 表结构修改
将之前修改过DDL的表进行修改，使用alter table tablename drop column columnname; 删除修改成varchar的列，再使用alter table table_name rename column column_name to new_column_name;将temp列名字修改
示例如下：
修改前：

tb1

| d_record_id | temp1 | temp2 | d_datetime_met | d_foretime_met|
| ----------- | ----- | ----- | ---------------- | ---------------- |
修改语句：
```sql
--alter table tablename drop column d_datetime_met;
--alter table tablename drop column d_foretime_met;
alter table table_name rename column d_datetime_met to d_datetime_met_varchar;
alter table table_name rename column d_foretime_met to d_foretime_met_varchar;
alter table table_name rename column temp1 to d_datetime_met;
alter table table_name rename column temp2 to d_foretime_met;
```
修改后：
tb2

| d_record_id | d_datetime_met | d_foretime_met |
| ----------- | ---------------- | ---------------- |

对所有修改过DDL的表进行表结构修改后即完成了数据迁移
detach_func:
```sql
create or replace function detach_par_subtable(tablename_ varchar) returns int

as

$$

DECLARE

sub_table RECORD;

begin

for sub_table in

select inhrelid::regclass as table_name from pg_catalog.pg_inherits where inhparent::regclass = (select tablename_::regclass)

loop

execute format('alter table %s detach partition %s',tablename_,sub_table.table_name);

RAISE NOTICE 'detach % ',sub_table.table_name;

end loop;

return 1;

end

$$

LANGUAGE 'plpgsql';
```

```sql
-- calculate ref_table size

copy(SELECT split_part(table_size.ref_name,'.',2) as ref_name

,table_size.size

FROM

(

SELECT ref_table.table_name AS ref_name,table_size(ref_table.table_name) AS size

FROM

(

SELECT a.schema_table AS table_name

FROM pg_dist_partition,

(

SELECT (quote_ident(pg_tables.schemaname) || '.' || quote_ident(pg_tables.tablename)) AS schema_table

FROM pg_tables

) AS a

WHERE partmethod = 'n'

AND pg_dist_partition.logicalrelid::regclass = a.schema_table::regclass

) AS ref_table

) AS table_size) to ref_table.csv with csv;

  

-- dist table np

copy(SELECT split_part(table_size.dist_table_name,'.',2) as table_name

,table_size.size

FROM

(

SELECT dist_table.table_name AS dist_table_name,table_size(dist_table.table_name) AS size

FROM

(

SELECT a.schema_table AS table_name

FROM pg_dist_partition,

(

SELECT (quote_ident(pg_tables.schemaname) || '.' || quote_ident(pg_tables.tablename)) AS schema_table

FROM pg_tables

) AS a

WHERE partmethod = 'h'

AND pg_dist_partition.logicalrelid::regclass = a.schema_table::regclass

and a.schema_table::regclass not in (select inhparent from pg_catalog.pg_inherits)

and a.schema_table::regclass not in (select inhrelid from pg_catalog.pg_inherits)

) AS dist_table

) AS table_size) to dist_table_np.csv with csv;

  

-- dist table p

  

copy (select parent_table.parent_id::regclass as parent_name,SUM(table_size(parent_table.table_name)) as table_size from

(

SELECT b.inhparent as parent_id, a.schema_table as table_name

FROM pg_dist_partition,

(

SELECT (quote_ident(pg_tables.schemaname) || '.' || quote_ident(pg_tables.tablename)) AS schema_table

FROM pg_tables

) AS a,

(select inhparent,inhrelid from pg_catalog.pg_inherits) as b

WHERE partmethod = 'h'

and pg_dist_partition.logicalrelid::regclass = a.schema_table::regclass

and a.schema_table::regclass = b.inhrelid::regclass

) AS parent_table

group by parent_name) to dist_table_p.csv with csv;
```

```sql
alter database database_name rename to new_name;
SELECT run_command_on_workers($cmd$ ALTER DATABASE database_name rename to new_name; $cmd$);
```
# 03: 实操
## A. 分布式策略
#### 背景

#### 西安库策略
- 大于100G的复制表改为分布式表
- 小于1G的分布式表改为复制表
- limit1, shard < n-wk* max_connections-wk/max_concurrent-q 20 * q(16~100/1~10)(1.6~100) 32~2000 
- limit2, shard < n-wk * cpu* q(1) 1280 - shard下限 max(limit1, limit2) 
	- limit1, shard >cpu (运算) (单机和多节点, 多节点优于单机)  128
	- limit2,shard > wk(存储) 
- 64-1280 
- shard size/shard >100M 
- 参考值 
	- 内存值. 
	- 30GB一个分区. 
	- ssd 
		- 高频IUD. 5000万. 
		- 少量更新, append only. 5亿.

| size(GB) | shard count |
| -------- | ----------- |
| [100,)   | 1280        |
| [10,100) | 200         |
| [1,10)         |        20     |

####  n2库策略
- 大于100G的复制表改为分布式表
- 小于1G的分布式表改为复制表
- 1280 = worker*cpu
- 10 = worker * 1
- 100 = 10 / 0.1 G
10 * cn_max_conn / wk_max_conn 50 

| size(GB) | shard count |
| -------- | ----------- |
| [100,)   | 1280        |
| [10,100) | 100         |
| [1,10)   | 10          |

#### 实际操作
1. 导出数据库数据
```sql
nohup pg_dump -a -Z 0 -d babj_pgdb --exclude-table-data=public.nafp_cra25_glb_bou_yer_tab --format=directory --jobs=4 -f /mnt/pg_bak/158/n2_bak_data/babj_pgdb_dir -v > /mnt/pg_bak/158/n2_bak_data/babj_pgdb.log 2>&1 &
```
2. 导出数据库表定义
	1. 去掉索引
		- `cat all_n15.outbak|grep -i create index > create_index.sql`
		- 替换pg_dumpall导出文件中的create index 为 --create index
	2. 修改mettime
		- 执行mettime_change.py，获得两个sql文件
3. 利用脚本修改分布式表相关定义
4. 导入ddl
	1. 导入all_n15_noindex.out
		- 删除database前，需要drop extension citus;
		- 在每个wk上创建database，extension
	2. 执行mettime_change_before.sql
	3. 执行citus_create_distributed_table.sql
5. 对比ddl
	1. 将pg_dist_partition,pg_dist_placement,pg_dist_node,pg_dist_shard导出
		```sql
		copy table_name to 'table_name.csv';
		```
6. patronictl -c edit-config 添加`max_wal_size: 20GB'
# 04: 问题
1. 执行pg_dump导出数据报错`input function 20175152 returned NULL`
	- 发现mettime类型部分值有问题，向简巨反应，只剩下`nafp_cra25_glb_bou_yer_tab`这个表和他的字表存在问题，暂定先跳过该表和他的字表，继续数据导出
2. postgres库存在很多分布式表，字表不存在，跳过该库
3. 创建分布式表的时候，role，extension，在cn存在worker不存在，需要手动操作
	- 
4. 分区表改复制表失败
	```bash
	nafp_bcccsm12_mul_day_tab
	surf_cra_glb_bou_day
	surf_bcccpsv3_glb_mul_day_tab
	nafp_ncep_glb_mul_pen_tab
	```
	- 解决办法，修改为分布式表
5. 分布式列和唯一索引冲突
	```bash
	const_nafp_cra_glb_bou_day_tab
	const_nafp_cra_glb_mul_day_tab
	const_nafp_derf2_glb_mul_hh_tab
	```
	- 解决办法，修改唯一索引，添加d_record_id![[create_citus_table.sql_func1]]
		```sql
		-- 修改const_nafp_cra_glb_bou_day_tab
			alter table ONLY public.const_nafp_cra_glb_bou_day_tab drop CONSTRAINT const_nafp_cra_glb_bou_day_tab_index;
		ALTER TABLE ONLY public.const_nafp_cra_glb_bou_day_tab
			ADD CONSTRAINT const_nafp_cra_glb_bou_day_tab_index UNIQUE (d_record_id, d_datetime, v_ele_code, v_level, v_evn, d_foretime);
		
		-- 修改const_nafp_cra_glb_mul_day_tab
		ALTER TABLE ONLY public.const_nafp_cra_glb_mul_day_tab
			drop CONSTRAINT const_nafp_cra_glb_mul_day_tab_index;
		ALTER TABLE ONLY public.const_nafp_cra_glb_mul_day_tab
			ADD CONSTRAINT const_nafp_cra_glb_mul_day_tab_index UNIQUE (d_record_id, d_datetime, v_ele_code, v_level, v_evn, d_foretime);
		
		-- 修改const_nafp_derf2_glb_mul_hh_tab
		ALTER TABLE ONLY public.const_nafp_derf2_glb_mul_hh_tab
			drop CONSTRAINT const_nafp_derf2_glb_mul_hh_tab_bkp_index;
		ALTER TABLE ONLY public.const_nafp_derf2_glb_mul_hh_tab
			ADD CONSTRAINT const_nafp_derf2_glb_mul_hh_tab_bkp_index UNIQUE (d_record_id, d_datetime, v_ele_code, v_level, v_evn, d_foretime);
		```
6. `too many open files`
	- 永久解决
		```bash
		vim /etc/security/limits.conf # 添加如下的行 
		* soft nproc 65536 
		* hard nproc 65536 
		* soft nofile 65536 
		* hard nofile 6553
		# 进程需要再修改后重启
		```
	- 动态修改
		```bash
		# CentOS7系统使用命令 
		prlimit --nofile=65536:65536 --pid 39977 
		# CentOS6系统使用命令 
		echo - n "Max open files=65535:65535" > /proc/39977/limits
		 ```

| table                           | size    |
| ------------------------------- | ------- |
| nafp_bcccsm12_mul_day_tab       | 4768 kB |
| surf_cra_glb_bou_day            | 5160 kB |
| surf_bcccpsv3_glb_mul_day_tab   | 18 MB   |
| nafp_ncep_glb_mul_pen_tab       | 435 MB  |
| const_nafp_cra_glb_mul_day_tab  | 127 GB  |
| const_nafp_cra_glb_mul_day_tab  | 252 GB  |
| const_nafp_derf2_glb_mul_hh_tab | 448 GB  |



```bash
babj_pgdb=# select pg_size_pretty(table_size('nafp_bcccsm12_mul_day_tab'));
 pg_size_pretty 
----------------
 4768 kB
(1 行记录)

babj_pgdb=# select pg_size_pretty(table_size('surf_cra_glb_bou_day'));
 pg_size_pretty 
----------------
 5160 kB
(1 行记录)

babj_pgdb=# select pg_size_pretty(table_size('surf_bcccpsv3_glb_mul_day_tab'));
 pg_size_pretty 
----------------
 18 MB
(1 行记录)

babj_pgdb=# select pg_size_pretty(table_size('nafp_ncep_glb_mul_pen_tab'));
 pg_size_pretty 
----------------
 435 MB
(1 行记录)

babj_pgdb=# select pg_size_pretty(table_size('const_nafp_cra_glb_bou_day_tab'));
 pg_size_pretty 
----------------
 127 GB
(1 行记录)

babj_pgdb=# select pg_size_pretty(table_size('const_nafp_cra_glb_mul_day_tab'));
 pg_size_pretty 
----------------
 252 GB
(1 行记录)

babj_pgdb=# select pg_size_pretty(table_size('const_nafp_derf2_glb_mul_hh_tab'));
 pg_size_pretty 
----------------
 448 GB
(1 行记录)
```