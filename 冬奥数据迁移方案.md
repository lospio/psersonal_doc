#author/zhangzhilingyun 
# TODO
- [ ] 添加物理部署方案
- [x] 细化分布式表分布列构建理由
- [ ] 验证同一组worker和cn两套分布式方案
- [ ] 查看Citus代码
# 1.项目背景
- ## 1.1 数据大小

|站点|当前大小|每月写入数据(条数)|
|:--|:--|:--|
|10.40.80.146:6543|125GB|165033681840|
|10.40.80.147:6543|79GB|4442066640|
|10.40.80.148:6543|41GB|1615296960|
- ## 1.2 主要业务
	气象要素的处理以及查询分析业务
	- Query
		```sql
		SELECT  latest_time,   free_tb_name, file_name,tb_name  FROM  nc_value_tb_info  WHERE  element_type = ? and data_code = ? 
		
		SELECT value_list FROM ${tbName} WHERE id = #{id} 
		
		SELECT tb_name AS tbName, latest_time AS latestTime,nx1,ny1,la1,lo1 FROM nc_value_tb_info WHERE element_type = 'PRE' and data_code = 'SURF_CMPAS_CHN_1KM_RT' 
		
		SELECT tb_name AS tbName, latest_time AS latestTime FROM nc_value_tb_info WHERE element_type = 'TAIR' 
		
		SELECT tb_name AS tbName, latest_time AS latestTime FROM nc_value_tb_info WHERE element_type = 'UWIN' 
		
		SELECT tb_name AS tbName, latest_time AS latestTime FROM nc_value_tb_info WHERE element_type = 'VWIN' 
		
		SELECT tb_name AS tbName, latest_time AS latestTime FROM nc_value_tb_info WHERE element_type = 'QAIR' 
		
		select id,value_list AS valueList from ${tableName} where id in ()
		
		select a.id,a.value_list,b.lon,b.lat from ${tableName} a,${gribTbName} b  
		where b.value in () and a.id = b.id
		
		SELECT id AS dbIndex,lon AS x,lat AS y from ${tableName} WHERE st_intersects(ST_SetSRID(ST_MakePoint(lon,lat),4326), st_geomfromgeojson(#{geojson}))
		
		select a.name,a.zh_name as zhname,a.values as "value" ,a.time_interval as timeInterval,a.type,a.update_time as  
		updateTime  
		FROM element_pre_5km_values a,gis_b_chn_basin_one_level_polygon b where a."name" = b.wrrcd
		
		SELECT st_area(st_transform(geom, 4527))/1000000 as area,code,"name",geojson  
		FROM "tb_xjdistrict0820"  
		where "level" = 2 and code != '100000'
		
		SELECT st_area(st_transform(geom, 4527))/1000000 as area,code,"name",geojson  
		FROM "region_division_cn"
		```
- ## 1.3 方案分析
	- 数据写入不构成性能瓶颈
	- 表容量大小当前250G,总量2T，预估占用3T

# 2 当前状态
- ## 2.1 磁盘状态
>/mnt/disk2 57% 4.6T
/mnt/disk1 63% 3.9T
/mnt/disk2 56% 4.6T
/mnt/disk1 59% 4.4T
/mnt/disk1 58% 4.4T
/mnt/disk2 54% 4.8T
/mnt/disk2 52% 5.0T
/mnt/disk1 55% 4.7T
/mnt/disk1 54% 4.8T
/mnt/disk2 63% 3.9T
/mnt/disk1 47% 5.6T
/mnt/disk2 45% 5.7T
/mnt/disk1 50% 5.2T
/mnt/disk2 40% 6.2T
/mnt/disk1 45% 5.7T
/mnt/disk2 39% 6.4T
/mnt/disk1 44% 5.9T
/mnt/disk2 39% 6.4T
/mnt/disk1 43% 5.9T
/mnt/disk2 39% 6.4T
/mnt/disk1 43% 5.9T
/mnt/disk2 39% 6.4T
/mnt/disk1 43% 5.9T
/mnt/disk2 39% 6.4T
/mnt/disk1 43% 5.9T
/mnt/disk2 38% 6.4T
/mnt/disk1 43% 6.0T
/mnt/disk2 38% 6.4T
/mnt/disk1 43% 6.0T
/mnt/disk2 38% 6.5T
/mnt/disk1 43% 6.0T
/mnt/disk2 38% 6.5T
/mnt/disk1 43% 6.0T
/mnt/disk2 45% 5.8T
/mnt/disk1 15% 8.8T
/mnt/disk2 89% 1.2T
/mnt/disk1 1% 11T
/mnt/disk2 15% 8.9T
/mnt/disk2 3% 10T
/mnt/disk1 1% 11T

# 方案描述

### 方案一 使用PostgreSQL本地表
在单个CN或者worker上创建本地表，将数据迁移到本地表
- #### 优点
	1. 可以把当前的业务平滑的迁移过来，拥有相近的架构和性能
- #### 缺点
	1. 导致单一磁盘剩余空间压力
### 方案二 使用现有的数据库集群
在现有的一套集群上创建一个新的数据库承载冬奥会的业务
- #### 优点
	1. 迁移方便，有可以即时使用的环境，只需要做数据迁移
	2. 分布式，主备功能完善
- #### 缺点
	1. 增加CN业务压力，可能存在性能瓶颈
### 方案三 新建一个分布式集群
利用现有的物理机和PostgreSQL实例新建一个分布式数据库集群
- #### 优点
	1. 隔离业务
	2. 与现有业务使用不同的CN，有更优越的性能表现
	3. 可以单独管理，扩展
- #### 缺点
	1. 现有每个物理机上存在两个PostgreSQL实例，使用的交叉主备，再添加新的实例会增加系统复杂度
	2. 使用现有的实例可能会造成数据冲突
- #### 实验
	1. 在三个物理机上构建三个PostgreSQL实例，命名为pg0 ，pg1，pg2
	2. 使用同一个数据库，将pg0作为cn，pg1，pg2作为两个worker
	3. 在pg0上新建一个表tt(id int,name char),将tt拓展为分布式表，分布列为id
	4. 在pg1作为cn，pg0，pg2作为两个worker
	5. 在pg1上新建表tt(id int, name char),将tt拓展为分布式表，分布列为id
	6. 报错，提示pg2上已经存在同名的表
	7. 将tt拓展为分布式表，分布式列改为name，成功
#### 物理部署

使用分布式表
### 分布式方案表定义
- `nc_value_tb_info` 分布式表 分布式列为 `element_type`
	- sql
		```sql
		SELECT  latest_time,   free_tb_name, file_name,tb_name  FROM  nc_value_tb_info  WHERE  element_type = ? and data_code = ? ;
		
		SELECT tb_name AS tbName, latest_time AS latestTime,nx1,ny1,la1,lo1 FROM nc_value_tb_info WHERE element_type = 'PRE' and data_code = 'SURF_CMPAS_CHN_1KM_RT' ;

		SELECT tb_name AS tbName, latest_time AS latestTime FROM nc_value_tb_info WHERE element_type = 'TAIR' ;
		
		SELECT tb_name AS tbName, latest_time AS latestTime FROM nc_value_tb_info WHERE element_type = 'UWIN' ;
		
		SELECT tb_name AS tbName, latest_time AS latestTime FROM nc_value_tb_info WHERE element_type = 'VWIN' ;
		
		SELECT tb_name AS tbName, latest_time AS latestTime FROM nc_value_tb_info WHERE element_type = 'QAIR' ;
		```
	- 定义
		```sql
		SELECT create_distributed_table('nc_value_tb_info', 'element_type');
		```
- `element_record`分布式表 分布式列为`element_type`
	- sql
		```sql
		SELECT * FROM element_record WHERE element_type = #{elementType} ORDER BY prediction_time ASC

		SELECT * FROM element_record WHERE element_type = #{elementType} AND prediction_time = #{predictionTime}
		
		SELECT * FROM element_record WHERE element_type = #{elementType} ORDER BY prediction_time DESC LIMIT 1
		
		SELECT element_type FROM element_record GROUP BY element_type
		
		SELECT id, element_type, produce_time, prediction_time, tb_name, file_local_path FROM element_record WHERE element_type = ?  prediction_time = ?
		```
	- 定义
		```sql
		SELECT create_distributed_table('element_record', 'element_type');
		```
- `${tableName}`分布式表 分布列为id
	- sql
		```sql
		SELECT value_list FROM ${tbName} WHERE id = #{id}

		select id,value_list AS valueList from ${tableName} where id in ()

		select a.id,a.value_list,b.lon,b.lat from ${tableName} a,${gribTbName} b  where b.value in () and a.id = b.id
		
		SELECT id AS dbIndex,lon AS x,lat AS y from ${tableName} WHERE st_intersects(ST_SetSRID(ST_MakePoint(lon,lat),4326), st_geomfromgeojson(#{geojson}))

		SELECT value_list FROM ${tbName} WHERE id = #{id}

		select id,value_list AS valueList from ${tableName} where id in ()
		
		select a.id,a.value_list,b.lon,b.lat from ${tableName} a,fm_grib5km b where b.value in () and a.id = b.id**
		```
	- 定义
		```sql
		SELECT create_distributed_table('${tableName}', 'id');
		```
- `${gribTbName}`分布式表 分布列为`id`
	- sql
		```sql
		select a.id,a.value_list,b.lon,b.lat from ${tableName} a,${gribTbName} b  where b.value in () and a.id = b.id
		```
	- 定义
		```sql
		SELECT create_distributed_table('${gribTbName}', 'id');
		```
- `element_pre_5km_values`分布式表 分布列为`name`
	- sql
		```sql
		select a.name,a.zh_name as zhname,a.values as "value" ,a.time_interval as timeInterval,a.type,a.update_time as  updateTime  FROM element_pre_5km_values a,gis_b_chn_basin_one_level_polygon b where a."name" = b.wrrcd
		```
	- 定义
		```sql
		select create_distributed_table('element_pre_5km_values','name');
		```
- `gis_b_chn_basin_one_level_polygon`分布式表 分布列为`wrrcd`
	- sql
		```sql
		select a.name,a.zh_name as zhname,a.values as "value" ,a.time_interval as timeInterval,a.type,a.update_time as  updateTime  FROM element_pre_5km_values a,gis_b_chn_basin_one_level_polygon b where a."name" = b.wrrcd
		```
	- 定义
	```sql
	select create_distributed_table('gis_b_chn_basin_one_level_polygon','name');
	```
- `tb_xjdistrict0820`分布式表 分布列为`level`
	- sql
		```sql
		SELECT st_area(st_transform(geom, 4527))/1000000 as area,code,"name",geojson  FROM "tb_xjdistrict0820"  where "level" = 2 and code != '100000'
		```
	- 定义
		```sql
		select create_distributed_table('tb_xjdistrict0820','code');	
		```
- `region_division_cn`分布式表 分布列暂定
	- sql
		```sql
		SELECT st_area(st_transform(geom, 4527))/1000000 as area,code,"name",geojson  FROM "region_division_cn"
		```


# 数据导入导出操作
1. 数据导出
利用pg_dump分别备份定义语句和数据，分两次导入pg10;暂存为单机表
- `pg_dump` [_`connection-option`_...] [_`option`_...] [_`dbname`_] 
	- 参数
		- -c 清除现存的数据和定义
		- -a 只转储表数据，大对象和序列值
		- -s 只转储ddl语句
		- -C 创建数据库
		- --if-exists 删除对象时使用if exists条件筛选
		- -F _format_  
		Selects the format of the output. _`format`_ can be one of the following:
			- p
			plain
			Output a plain-text SQL script file (the default).
			- c
			custom
			Output a custom-format archive suitable for input into pg\_restore. Together with the directory output format, this is the most flexible output format in that it allows manual selection and reordering of archived items during restore. This format is also compressed by default.
			- d 
			directory
			Output a directory-format archive suitable for input into pg\_restore. This will create a directory with one file for each table and blob being dumped, plus a so-called Table of Contents file describing the dumped objects in a machine-readable format that pg\_restore can read. A directory format archive can be manipulated with standard Unix tools; for example, files in an uncompressed archive can be compressed with the gzip tool. This format is compressed by default and also supports parallel dumps.
			- t 
			tar
			Output a `tar`\-format archive suitable for input into pg\_restore. The tar format is compatible with the directory format: extracting a tar-format archive produces a valid directory-format archive. However, the tar format does not support compression. Also, when using tar format the relative order of table data items cannot be changed during restore.
	- 定义语句导出
		- 导出指定数据库的定义语句
			```sql
			pg_dump [connection_option] -c -C -s -F p --if-exists [database] > ddl.out
			```
		- [[weather_station.ddl]]
		- 修改ddl.out 将create database 语句中local改为lc_ctype
		- 导出指定数据库的数据
			```sql
			pg_dump [connection_option]  -C -a -F d   [database] -f [outdir]
			```
		- 导入指定数据库定义语句
			```sql
			psql [connection_option] -f ddl.out
			```
		- 导入指定数据库的数据 