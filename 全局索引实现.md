# 0 计划
- [ ] 验证方案可行性
	- 新建一个虚表，二级索引和主键索引不在一个文件，是否可行，通过table_per_file验证
- [ ] 并行
	- [ ] 如何加锁
	- [ ] GSI和普通二级索引冲突模型是否一致，差异在哪里，性能差异

![[Pasted image 20240513151136.png]]
![[Pasted image 20240513151210.png]]
```text
[mysqld]
user=root
datadir=/root/sync/mysql/data
sql_dialect=oracle
innodb_file_per_table=false
innodb_data_file_path=ibdata1:10m;ibdata2:10m;ibdat3:10M:autoextend
innodb_data_home_dir=/root/sync/mysql/data
```
```sql
create table t (a int, b char(10));
insert into t values(1,'aaa');
insert into t values(2',bbb');
insert into t  values(3,'ccc');
update t set b = 'abc' where a = 1;
```

![[Pasted image 20240513161909.png]]
![[Pasted image 20240513162242.png]]
## lock
### 行级锁
不需要额外关注，二者之间没有差异，因为在record层级，忽略了table_space以及文件的概念，全部以page为处理单元；相对于普通的二级索引，GSI的操作一致，抽象逻辑为定位到某一个page，找root，这些信息从dict_index_t中可以获取。该元信息则是在创建索引时生成。
### 表级锁
在需要锁表的时刻，