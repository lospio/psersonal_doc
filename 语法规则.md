# 概述
本文尝试介绍PG和VB中的语法解析过程。在讲解以前，我给自己准备了如下的几个文档，希望本文可以做出解答。
1. 一条sql如何被识别为合法？
2. 一条sql如何被识别为非法？
3. 如何增加一条合法sql？
4. 每个报错的SQL的语句如何定位？
5. 每个语法功能如何实现？

---
# 语法解析过程

![[Pasted image 20251009151019.png]]
如图所示，SQL经过五个子系统的顺序操作最终得到了查询结果，这之中的第一步就是语法解析，即将SQL转换为计算机可理解并执行的指令的过程，主要包括**词法分析**、**语法分析**以及**语义分析**三个主要阶段。
#### 词法分析 Lexical Analysis
- 任务：扫描 SQL语句,将其分解为一个个独立的单元，称为"token"
- 示例：将 `SELECT * FROM users;` 分解为 `SELECT`、`*`、`FROM`、`users` 和 `;` 这几个 Token
#### 语法分析 Syntax Analysis      
- 任务： 根据SQL的语法规则（通常由上下文无关文法定义），检查Token序列的结构是否符合规范，判断语句在结构上是否正确
- 工具：Bison
- 结果：如果语句结构正确，会生成一个数据结构来表示语句的语法树；如果错误，则会报告语法错误。
- 示例：

	```cpp
	// 四则运算的语法规则
	%%
	
	E   :   E '+' E '\n'        { printf("this is addtion.\n"); }
			|   E '-' E           { printf("this is subtraction.\n"); }
			|   E '*' E           { printf("this is multiplication.\n"); }
			|   E '/' E           { printf("this is division.\n"); }
			|   T_NUM        { printf("this is a number.\n");}
			|   '(' E ')'          { printf("this is parenthesis.\n"); }
			;
	
	%%
	
	// 合法 
	2 + 3 
	2
	( 2 )
	2 + 3 + 3
	//非法
	2 ++ 3
	2 3 +
	```

>	bison 里面 ”:” 代表一个 “->” ，同一个非终结符的不同产生式用 “|” 隔开，用 ”;” 结束表示一个非终结符产生式的结束；每条产生式的后面花括号内是一段 C 代码、这些代码将在该产生式被应用时执行，这些代码被称为 action ，产生式的中间以及 C 代码内部可以插入注释（稍后再详细解释本文件中的这些代码）；产生式右边是 ε 时，不需要写任何符号，一般用一个注释 /* empty */ 代替。

### 语义分析
- **任务**: 在语法正确的基础上，对SQL语句的上下文进行审查，进行语义层面的判断。
- **具体工作**:
    - 检查引用的表名是否存在
    - 检查列名是否属于相应的表
    - 判断操作符和操作数类型是否匹配（例如，不能对字符串使用数学运算）。- 检测语义上的二义性
    - ...
---
# 实现原理
Parser中使用flex完成词法分析，将scan.l进行编译生成词法分析的cpp代码。使用Bison完成语法分析工作。
## Flex
flex的文件内容分为三个部分
```text
/* P1: declarations(定义段) */
%{
  
%}

%%
  /* P2: translation rules(规则段) */
%%

/* P3: auxiliary functions(用户辅助程序段，c函数)*/
```
**定义段** 包括文字块、定义、内部声明等。  
C语言的头文件、函数和变量的声明等一般就放在%{…%}之间，这一部分的内容会被直接复制到生成.c文件的开头部分。
包含%option选项

```text
%option noyywrap /* 定义段中包含了option选项*/

%{
#include "cal.tab.h"
extern int yylval;
%}
```

**规则段** %%...%%之间部分，为一系列匹配模式(**正则表达式**)和动作(**C代码**)。

当flex扫描程序运行时，它把输入与规则段的模式进行匹配，每次发现一个匹配（被匹配的输入称为标记（token））时就执行与那种模式相关的C代码。

```text
pattern（正则表达式） { action(c代码) }

example:
[0-9]+ {yylval = atoi(yytest); return NUMBER;}
```

**用户辅助程序段** 为C代码，会被原样复制到c文件中，一般这里定义一些辅助函数等。

```text
int terror(chr *s)
{
    printf("%s\n", s);
    return 0;
}
```
## Bison
Bison采用自底向上的分析方法，实现了LR(1)的一个子集LALR(1)。
#### 文件内容
```text
/*P1: declarations 定义段*/
%{

%}
 
%%
/*P2: grammar rules 规则段(rule-action)*/
    
     A: a1  {  语义动作1 }
	  | a2  {  语义动作2 }
	  | …
	  | an  {  语义动作n }
	  | b  //没有{…},则使用缺省的语义动作       
	; //产生式结束标记
    //语义动作一般是在产生式右部分析完，归约动作进行前执行。
    A→ a1 | a2 |  … | an | b 
%%

/* P3: supporting C routines 用户辅助程序段(C函数) */
```
**定义段** 可以分为两部分：

1. %{ 和%}之间的部分，C语言编写的，包括头文件include、宏定义、全局变量定义、函数声明等;

2. 对文法的终结符和非终结符做一些相关声明。

常用的Bison标记声明有：%token %union %start %type %left %right %nonassoc等。

> %token 定义文法中使用了哪些终结符。定义形式： %token TOKEN1 TOKEN2  
> 终结符一般全大写；（如 TOKEN1 TOKEN2）  
> 一行可定义多个终结符，空格分隔；  
>   
> %left、%right、%nonassoc 也是定义文法中使用了哪些终结符。定义形式与%token类似。  
> 先定义的优先级低，最后定义的优先级最高，同时定义的优先级想通过。  
> %left表示左结合，%right表示右结合；  
> %nonassoc 表示不可结合（即它定义的终结符不能连续出现。例如<，如果文法中不允许出现形如a<b<c的句子，则<就是不可结合的）  
>   
> %left AA BB  
> %nonassoc CC  
> %right DD  
> 表示优先级关系为：AA=BB<CC<DD，表示结核性为：AA\BB左结合， DD右结合， CC不可结合。  
> 注意：也可以于%prec来结合使用来改变token的优先级和结合性
> %union 声明语法符号的语义值类型的集合，  
  bison中每个符号包括记号和非终结符都有一个不同数据类型的语义值，并使用yylval变量在移进和归约中传递这些值，

**用户辅助程序段** 为C代码，会被原样复制到c文件中，这里一般自定义一些函数。
#### LR分析器
在编译原理中，LR 的意思是 **“**==**由左（Left）至右扫描输入串，进行最右推导（Rightmost derivation）**==**”**。更具体地说，LR 分析法是一种自底向上的语法分析方法，通过从输入串的左侧开始扫描，并在分析过程中进行自底向上的规约来构建语法树。
自底向上语法分析的核心思想是使用移入——归约法（shift——reduce)。在这个过程中使用一个寄存符号的栈来辅助分析。<font color="#ff0000">输入符号移入栈的过程叫做移入</font>，<font color="#ff0000">当栈顶形成某个产生式的候选式时，会替换为该产生式的左部符号，这个替换过程称为规约</font>。
#### 移入-规约法
基本步骤如下：
1. **移入（Shift）**：将下一个输入符号移入到栈的顶部。如果**输入符号**与**栈顶的符号**能够匹配某个产生式的右部，则进行归约操作；否则，继续移入下一个**输入符号**。
2. **归约（Reduce）**：当**栈顶的符号串**与某个产生式的右部相匹配时，将该**符号串**替换为产生式的**左部符号**。归约操作是自底向上语法分析中的关键步骤，它使得语法分析器能够逐步构建出语法树。
在移入-规约过程中，语法分析其会不断的根据栈顶和输入符号的情况进行移入和规约操作，知道栈中只剩下文法的开始符号，此时语法分析成功完成。

---
# 例子
babelfih修改过程中有如下语法报错：
![[企业微信截图_2b33931d-2bf9-44bc-bf09-d18e433696c6.png]]
#### 解题思路
1. 查看create table中关于列identity属性的语法规则，查看是否匹配
	```cpp
	// IDENTITY这里会移位，然后对tsql_OptParenthesizedIdentList进行规约
	| IDENTITY_P tsql_OptParenthesizedIdentList  
  {  
   Constraint *n = makeNode(Constraint);  
   if (sql_dialect != SQL_DIALECT_TSQL)  
    ereport(ERROR,  
      (errcode(ERRCODE_SYNTAX_ERROR),  
       errmsg("This syntax is only valid when babelfishpg_tsql.sql_dialect is TSQL"),  
       parser_errposition(@1)));  
  
   TSQLInstrumentation(INSTR_TSQL_IDENTITY_COLUMN);  
   n->contype = CONSTR_IDENTITY;  
   n->generated_when = ATTRIBUTE_IDENTITY_ALWAYS;  
   n->options = $2;  
   n->location = @1;  
   $$ = (Node *)n;  
  }
  // 继续找tsql_OptParenthesizedIdentList的规则
  // 可以看到报错位置刚好处于Iconst的位置，Iconst在语法文件里面代表整形数字，这里报错意味着对1的类型判断出了问题，进一步去      查找词法解析函数
  tsql_OptParenthesizedIdentList:  
    '(' tsql_IdentList ')' { $$ = $2; }  
    | /*EMPTY*/ { $$ = NIL; }  
   ;  
  
	tsql_IdentList:  
	    NumericOnly ',' opt_plus Iconst  
	     {  
	      $$ = list_make3(makeDefElem("start", (Node *)$1, @1),  
	                      makeDefElem("increment", (Node *)makeInteger($4), @1),  
	                      makeDefElem("minvalue", (Node *)$1, @1));  
	     }  
	    | NumericOnly ',' '-' Iconst  
	     {  
	      $$ = list_make3(makeDefElem("start", (Node *)$1, @1),  
	                      makeDefElem("increment", (Node *)makeInteger(- $4), @1),  
	                      makeDefElem("maxvalue", (Node *)$1, @1));  
	     }  
	    ;  
  
	opt_plus:  
	    '+' {}  
	    | /* empty */ {}  
	   ;
	```
1. 查找词法解析对应的函数。发现迁移代码的时候if被注释了导致将 1 错误识别为Fconst出现了错误，问题解决。
![[企业微信截图_404a13de-d480-439a-a088-ae6a1ea5efec 1.png]]

#### 插叙 
看起来很容易。。。是的学会了之后很容易，但是在上手的过程中难免会走很多弯路。
1. 怀疑有移位规约冲突，debug查看bison输出得到如下冲突，进一步查看发现无法对应
	![[Pasted image 20251011160853.png]]
	![[Pasted image 20251015200106.png]]
2. 跟语法解析中的状态机，尝试找到问题
	![[企业微信截图_ebb046ec-ceb4-48d7-b0b9-f5baea6a3e2f.png]]
	![[企业微信截图_24ad2f83-13c8-44bb-b4ad-252f167c7e26.png]]
	找到了红圈位置发生了异常，但是不理解遂去补充知识。

	. yycheck[yyn]记录当前应该操作的yytoken
	. yytable[yyn]记录当前应该执行的state
3. 然后去翻state发现在这个位置对应的下一个语法完全对不上，于是红框的校验没有通过
4. 最后在大佬提醒下再次返回去看词法解析的值
---


鉴于你股市斩获颇丰，而我持仓亏损致咖啡断供。一杯咖啡可慰藉我受损心态，亦能彰显你盈利后分享格局，此笔支出具备充分必要性。