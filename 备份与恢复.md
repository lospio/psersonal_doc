## 基础备份
1. 连接数据库，设置`full_page_writes`为`on`
```sql
show full_page_writes;
#修改为on
vi postgresql.conf
```
2. 基础备份
```bash
pg_basebackup -D /mnt/pg_bak/141/base_backup -X stream -d dbname=postgres -U repl -h localhost -p 5432 --max-rate=100M  -v -P >> base_backup &
```
- -D 存放目录
- -U repl （从patroni.conf)中查看该用户名
- -h 备份源host
- -p 备份源port


## 时间点恢复
1. 停止pg
2. 将data目录下所有文件备份于temp location（至少备份pg_wal文件夹)
3. 清空data目录
4. 将基础备份文件拷贝至data下
5. 删除data下pg_wal所有文件
6. 将步骤2中备份的pg_wal文件拷贝至data下
7. 创建recovery.conf指定恢复的目标时间点
8. 启动pg
t1 启动base_backup
t2 结束base_backup
