## csfv2表临时改造
- 创建新表 sub_table_202212，sub_table_202212与csvf2表结构一致且添加约束——date在区间(2022120100 - 2022123100)内(全闭合区间)
- 开启事务：
	- 从旧分区表子表(指现有的时间范围是2020-2022的子表下面统一称这张表为sub_table_2020_2022)中查找所有符合条件的数据(符合上面提到的约束)插入到sub_table_202212中
	- detach sub_table_2020_2022
	- attach sub_table_202212
- 结束事务
- 备份 sub_table_2020_2022 
- drop sub_table_2020_2022
- 删除sub_table_202212上的约束(attach了以后就没有必要了)



## shard数不合理表改造
假设现在需要改造分布式表为table1，现有17个节点，但是table1的shard数小于17，会造成数据分布不均匀
根据table1目前大小以及集群处理器等信息计算出合适的shard_num
- 根据table1表结构创建新表new_table1，并且按月份创建分区表子表
- 开启事务
	- 从table1中读取最近三个月的数据并写入new_table1中
	- rename table1 to table1_old
	- rename new_table1 to table1
- 结束事务
- 备份table1_old
- drop table1_old