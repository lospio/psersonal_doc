# 方案
- 会话级资源限制 使用cgroup
- 表空间+quota限制磁盘
# 友商方案
## 1. openGauss
### A. 通过资源池管理资源
| 属性              | 说明                                                              |
| ----------------- | ----------------------------------------------------------------- |
| mem_percent       | 最大占用内存百分比。                                              |
| active_statements | 资源池允许的最大并发数。-1为不限制并发数量。                      |
| max_dop           | 开启SMP后，算子执行的并发度，保留参数。                           |
| memory_limit      | 内存使用上限，保留参数。                                          |
| io_limits         | 每秒触发IO的次数上限。行存单位是万次/s，列存是次/s。0表示不控制。 |
| cpu_affinity      | CPU亲和性，保留参数。                                             |
### B. 通过控制组关联资源池管理资源
| 属性   | 说明                    |
| ------ | ----------------------- |
| shares | 占父节点CPU资源的百分比 |
| limits | 占父节点CPU核数的百分比 |
| rate   | Timeshare中的分配比例   |
|  cpucores      |       CPU核心数                  |
控制组配置CPU占比
## 2. Greenplum 修改pg源码
- 基于role进行管控，控制该用户下某类变量的使用和总和
>The most common application for resource groups is to manage the number of active queries that different roles may execute concurrently in your Greenplum Database cluster. You can also manage the amount of CPU and memory resources that Greenplum allocates to each query.

### A. 资源组 基于ROLE
| 限制种类            | 说明                                                                                                                             |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| MEMORY_AUDITOR      | The memory auditor in use for the resource group.                                                                             |
| CONCURRENCY         | The maximum number of concurrent transactions, including active and idle transactions, that are permitted in the resource group. |
| CPU_RATE_LIMIT      | The percentage of CPU resources available to this resource group.                                                                |
| CPUSET              | The CPU cores to reserve for this resource group.                                                                                |
| MEMORY_LIMIT        | The percentage of reserved memory resources available to this resource group.                                                    |
| MEMORY_SHARED_QUOTA | The percentage of reserved memory to share across transactions submitted in this resource group.                                 |
| MEMORY_SPILL_RATIO  | The memory usage threshold for memory-intensive transactions. When a transaction reaches this threshold, it spills to disk.      |
- MEMORY_AUDITOR 指定资源组类别 
- CONCURRENCY 分配slot，等资源，等待队列
- CPU 通过控制CPU占用百分比或者指定CPU Cores控制
- Memory 四级管理 控制百分比
	规定了单个节点的占比区间，sum小于100；
	基于资源组的控制，会使用全局共享内存
	- Database node
	- segment
	- resource group
	- transaction
### B. 资源组的使用
- enable
- create
- monitor
- 管理事务
### C. 支持的资源隔离类别
-   active_statements， 该queue同时可以运行的query数量。
-   max_cost，指资源组内所有正在运行的query的评估成本的最大值。
-   cost_overcommit，当系统空闲时，是否允许该queue的query总cost超出设定的max_cost。
-   min_cost 指低于该值的QUERY不计入该queue 的cost成本，也不排队，而是直接执行。  
-   priority ， 用于平衡各个QUEUE之间的CPU争抢使用，分为5个等级，每个等级设定了响应的weight，间隔一定的时间判断使用的资源是否达到了weight，然后对该queue 的query使用pg_usleep进行抑制。
-   mem_limit ， 为队列中单个segment query(s)允许的最大statement(s)运行内存。
## 3. EDB
### A. 控制资源组
- 当前进程加入资源组
- 当前进程移除资源组
- 资源组分配给Role 、Database、Database Server
- 修改资源组
	- rename
	- 修改资源类型
	- 分配资源类型
- 删除资源组
- monitor
### B. CPU 设置为比例 0-1
>Set the `cpu_rate_limit` parameter to the fraction of CPU time over wall-clock time to which the combined, simultaneous CPU usage of all processes in the group should not exceed.
### C. Dirty Buffer Throttling 所有资源管理起每秒写入共享内存中的数量 单位 kb/s
>Set the `dirty_rate_limit` parameter to the number of kilobytes per second for the combined rate at which all the processes in the group should write to or “dirty” the shared buffers
### D. System Catalogs
- 表 `edb_all_resource_groups`

| Column                       | Type    | Description                                                                  |     |
| ---------------------------- | ------- | ---------------------------------------------------------------------------- | --- |
| active_process               | integer | Number of currently active processes in the resource group.                  |     |
| cpu_rate_limit               | float8  | Maximum CPU rate limit for the resource group. `0` means no limit.           |     |
| per_process_cpu_rate_limit   | float8  | Maximum CPU rate limit per currently active process in the resource group.   |     |
| dirty_rate_limit             | float8  | Maximum dirty rate limit for a resource group. `0` means no limit.           |     |
| per_process_dirty_rate_limit | float8  | Maximum dirty rate limit per currently active process in the resource group. |     |

- `edb_resource_group`

| Column             | Type   | Description                                                      |
| ------------------ | ------ | ---------------------------------------------------------------- |
| rgrpcpuratelimit   | float8 | Maximum CPU rate limit for a resource group. `0` means no limit. |
| rgrpdirtyratelimit | float8 | Maximum dirty rate limit for a resource group. `0` means no limit.                                                                 |


- 基于进程控制
- 每个资源组，控制一类资源，计算组内所有进程占用资源和
- 可以将资源组分配给role，datebase,  database serv  er instance
>- This capability allows you to protect the system from processes that may uncontrollably overuse and monopolize certain system resources.
>- A _resource group_ is a named, global group, available to all databases in an Advanced Server instance, on which various resource usage limits can be defined. Advanced Server processes that are assigned as members of a given resource group are then controlled by EDB Resource Manager so that the aggregate resource usage of all processes in the group is kept near the limits defined on the group.
>- Processes can be included by default in a resource group by assigning a default resource group to roles, databases, or an entire database server instance.
## 4. Oracle
- 限制**session级**cpu使用
- 给**用户**，**应用**分配不同的CPU占用时间比
- 限制一组用户的并行度
- Resource Consumer Groups 
	The Resource Manager allocates resources to consumer groups according to the set of resource plan directives (directives) that belong to the currently active resource plan.
### A. Elements of Resource Manager

| Element                 | Description                                                                                                                                                                               |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Resource consumer group | A group of sessions that are grouped together based on resource requirements. The Resource Manager allocates resources to resource consumer groups, not to individual sessions.           |
| Resource plan           | A container for directives that specify how resources are allocated to resource consumer groups. You specify how the database allocates resources by activating a specific resource plan. |
| Resource plan directive | Associates a resource consumer group with a particular plan and specifies how resources are to be allocated to that resource consumer group.                                              | 

### B. Assign Sessiongs to Resource Consumer Groups
- 创建session-Consumer groups的映射规则
- 更改session-Consumer groups的映射规则
- 删除session-Consumer groups的映射规则
- session-Consumer groups的映射规则基于session的某些属性区分，例如user name，service，application name .etc
- 规则定义优先级——解决冲突
- 两类属性：login attributes and run-time attributes
- switch时机：
	- first logs in
	- run-time attribute changed
- 预制Consumer Group Mapping Rules
- 自动切换
- 指定超出限制的行为
- 申请、收回switch 权限
### C. 资源  
- **CPU**
	- 分级1-8,2使用1未分配或者1分配未使用的资源

	| Consumer Group | Level 1 CPU Allocation | Level 2 CPU Allocation | Level 3 CPU Allocation |
	| -------------- | ---------------------- | ---------------------- | ---------------------- |
	| HIGH_GROUP     | 80%                    |                        |                        |
	| LOW_GROUP      |                        | 50%                    |                        |
	| MAINT_SUBPLAN  |                        | 50%                    |                        |
	| OTHER_GROUPS               |                        |                        |         100%               |

	- 设置每组的最大占用
- **Degree of Parallelism Limit** 
- **Parallel Target Percentage** 百分比
	>Use the `PARALLEL_TARGET_PERCENTAGE` directive attribute to specify the maximum percentage of the parallel server pool that a particular consumer group can use

- **Parallel Queue Timeout**
	>The `PARALLEL_QUEUE_TIMEOUT` directive attribute enables you to specify the maximum time, in seconds, that a parallel statement can wait in the parallel statement queue before it is timed out
	
- **Active Session Pool with Queuing** control the maximum number of concurrently active sessions allowed within a consumer group
- **Automatic Consumer Group Switching**
- **Canceling SQL and Terminating Sessions** 
- **Execution Time Limit**
- **Idle Time Limit**

## 5. Mysql
- 基于资源组控制资源，将不同的线程加入资源组，以实现资源控制
	>MySQL supports creation and management of resource groups, and permits assigning threads running within the server to particular groups so that threads execute according to the resources available to the group. Group attributes enable control over its resources, to enable or restrict resource consumption by threads in the group. DBAs can modify these attributes as appropriate for different workloads.
	
###  A. Resource Group Elements
- 创建
- 修改
- 删除
- assign threads 或者 invividual statements
<<<<<<< HEAD
=======
### B. Resource Group Attributes
- VCPU——CPU affinity，CPU Set
- thread priority——-20 (highest priority) to 19 (lowest priority)
>>>>>>> 3a67f88f5e5653b676b8b0f1fc35c24d57d8609e


# 参考
- https://developer.aliyun.com/article/58239
- https://tech.meituan.com/2015/03/31/cgroups.html
- https://zq99299.github.io/linux-tutorial/tutorial-basis/14/01.html#quota-%E7%9A%84%E4%BD%BF%E7%94%A8%E9%99%90%E5%88%B6
- [Greenplum](https://cubox.pro/my/card?id=ff8080818239414501823db7932f72f8)
- [EDB](https://www.enterprisedb.com/docs/epas/10/epas_guide/10_edb_resource_manager/?spm=a2c6h.12873639.article-detail.20.3dc2653aYdxmJi#pID0E0JJC0HA)
- [oracle](https://docs.oracle.com/cd/E11882_01/server.112/e25494/dbrm.htm#ADMIN11841)
- [mysql](https://dev.mysql.com/doc/refman/8.0/en/resource-groups.html)