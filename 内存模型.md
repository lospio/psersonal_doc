# PG
## 概述 
Postgres是进程架构，主进程是Postgres，它会在内存中分配共享内存区域，启动各种后台进程，如有必要还会启动复制相关进程与后台工作进程，并等待来自客户端的连接请求。每当接收到来自客户端的连接请求时，它都会启动一个后端进程，然后由启动的后端进程处理该客户端发出的所有查询。

![[Pasted image 20250925153245.png]]
## 全局Context
### TopMemoryContext
- 这是PG内存上下文中所有节点的父节点
- 从这里申请内存，类似于使用malloc，申请后不会被自动重置或者删除
- 这适用于应该永久存在的内容，或者手动清楚内存的内容，应该尽量避免如此操作。
- 尤其需要避免将CurretnMemoryContext指向此处
### PostmasterContext
- postmaster的工作上下文
- 当postmaster fork一个新的postgres后可以把Postmastercontext释放
- 需要在鉴权以后释放掉，因为pg_hba和pg_ident存在与内存上下文
### CacheMemoryContext
- 存储relcache，catcache和其他相关的模块
- 申请的内存不会被自动重置或者删除
### MessageContext
- 保存来自于前端的当前命令消息，以及与之相关的派生消息（parse and plan tree）
- 将在PostgresMain外层循环的每个循环开始时重置，并删除子上下文
- 每个transaction和每个portal分开
### TopTransactionContext
- 保存所有再顶层事务结束前有效的内容
- 每个顶层事务周期结束时重置
- 此处用于保存跨多个子事务状态的控制信息
- 其内容一直保留到事务块通过COMMIT/ROLLBACK退出为止
### CurTransactionContext
- 全局变量。保存着必须留到事务提交时的数据，尤其是toptransactioncontext需要的
- 当指向一个子事务时，如果子事务aborts，他会被丢弃；如果子事务commit，他会保留到toptransactioncontext commit
- 子事务终止时，必须与上层事务中保存的任何指针或者列表接触关联，否则上层提交时会崩溃
- palloc分配的空间处于该上下文
### PortalContext 
- 这是一个全局上下文，保存着每个活跃的portal
- 这里推荐存放生命周期与portal执行的周期一致的变量
### ErrorContext
- 在error recovery 中会切换到该上下文，再error recovery结束时重置
- 会始终保留几KB的内存，任何情况下都有内存用于错误回复
## 特定场景context
### Prepared Statements And Portals
- 一个prepared-statement对象有一个context，他对执行器是只读的。
- 一个execution-portal对象有一个context，活跃的context被PortalContext引用
- 由DECLARE CURSOR创建的的门户 ，context存有parse and plan tree
- 由prepared statements创建的门户，只需要使用预处理语句的树，实际不需要再context上分配空间
### Logical Replication Worker Contexts
- ApplyContext ，在apply worker生命周期内存在，也可以使用TopMemoryContext，但是为了便于分析问题使用
- ApplyMessageContext， 在处理每个逻辑复制协议消息后重置的短暂上下文。
### Transient Contexts During Execution
- 创建prepared statement时，parse 和 plan trees会在一个临时上下文上申请，该上下文是MessageContext的子上下文，目的是失败的时候可以自动删除。成功以后，会拷贝到私有的上下文，同时把临时的context释放。对于simple query不会拷贝和释放。
- 执行器在一个临时context里面运行，ExecutorStart申请，ExecutorEnd释放。该context里保存有plan state tree。context里申请的对象一般都会保留到查询结束。该context的先祖是PortalContext下的一个子per-portalcontext
- 执行器执行过程中，ExprContext会有一个关联的私人context用于表达式求值。拥有该context的Node不再需要结算结果时重置私人context。典型的做法实在每一个tuple-fetch循环的初始位置重置。
- btree和hash索引的比较函数需要注意内存泄露问题，该泄露内存再查询结束后才会被恢复
- 切换生效的CurrentMemoryContext时候，需要将其数据拷贝到调用着的context中。对于一个Node返回的tuple，现有的规则是在循环初始时重置，因此我们可以在结束时将在per-tuple context中分配的变量返回给调用者，知道获取下一个tuple或者被告知结束时才会失效。这里会讲结果从per-tuple context拷贝到per-query context
# Vastbase
## is_sealed机制
## 

# 参考
1. [Discussion on openGauss Memory Management](https://opengauss.org/en/blogs/2022/Discussion-on-openGauss-Memory-Management.html?utm_source=chatgpt.com)