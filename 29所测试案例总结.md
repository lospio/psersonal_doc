# 1. 设计方案
1. 设计空间查询及属性查询，覆盖客户所需场景
2. 构造测试数据
	- 建表语句
	- 构造数据
	- 测试query
	- 创建index语句
	- 选取shard_count,分布式列
3. 完善步骤，细化到每一步的操作，执行命令

# 2. 验证方案
1. 组内评审方案，给出意见，即时修改，重复评审
2. 搭建本地测试环境
3. 完全按照方案操作步骤执行，验证方案可行性与正确性
# 3. 性能测试
1. 在本地环境测试性能，摸底测试
2. 根据摸底测试结果，初步调优
3. 按照现有调优方案，在实际测试环境修改配置
4. 在实际测试环境进行性能测试
5. 组内评审性能测试结果，寻找差异点，难以接受的测试瓶颈点，争取调优解决
6. 调优过后重复性能测试，在符合客户要求的基础上找到更优解，经过组内评审完成测试报告，发给客户
# 4. 经验
1. citus AGG
	- count （distinct） 褪去agg函数，下推查询原始数据
	- avg 分为 sum 和 count，下推到worker
	- 其他pre-difined agg直接下推
2. citus 多个分布式表如何查询
	- 以某个分布式表为外表，将另外一个分布式表符合查询条件的tuples，按照某些规则重分布至外表所在的每个shard。
3. bitmap heap scan
	- 一次性取回所有符合条件的index ，拿到对应的line pointer，按照物理磁盘的存储顺序取回符合条件的tuples
	- recheck  二次筛选，首次筛选页，二次筛选具体到符合要求的每一页上的元组
4. 慢sql调优
	[[29所测案测试结果]]
5. shard_count 选择
[[citus shard_count选择]]
6. 客户要求
	- 在写入压力下测试读的效率
	- 写入tps在10000以上
	- 提高测试的并发数量
7. 对于分布式表数据写入性能测试，需要注意插入数据的shard分布，当前测试经验来看，每个worker使用一个shard效率最高
8. pgbench 程序和CN是否部署在同一节点
