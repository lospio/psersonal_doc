
| babelfish                                       | vb        |
| ----------------------------------------------- | --------- |
| 支持将default值放入第一个分区，即第一个分区除了定义的范围，还可以作为default分区 | 待确认是否支持   |
| 支持分区键里每个key单独设置collation,operation              | 不支持       |
| 分区键可以为非窗口函数的函数表达式，保留关键字，a_expr                  | 只支持a_expr |
| 支持foreign table的分区表                             | 不支持       |
 